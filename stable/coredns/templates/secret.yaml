{{- if .Values.envSecretName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.envSecretName }}
type: Opaque
data:
  {{- range $key, $val := .Values.env.secret }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}
{{- end }}